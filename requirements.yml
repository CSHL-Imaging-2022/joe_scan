#update with:   conda env update --file requirements.yml --prune
name: joe_scan
channels:
  - conda-forge
dependencies:
  - python=3.8
  - numpy
  - matplotlib
  - scipy
  - scikit-image
  - pip
  - pip:
     - nidaqmx
     - superqt
     - pyqtgraph
