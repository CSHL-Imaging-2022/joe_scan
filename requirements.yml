#update with:   conda env update --file requirements.yml --prune
name: joe_scan
channels:
  - anaconda
  - conda-forge
dependencies:
  - numpy
  - matplotlib
  - scipy
  - scikit-image
  - pip
  - pyqtgraph
  - pip:
     - nidaqmx
     - superqt
